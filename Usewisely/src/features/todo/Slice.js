import { createSlice,nanoid } from "@reduxjs/toolkit";

const initialState = ()=>{
    
}

const time = createSlice(
    {
        name:"timeandtask",
        initialState,
        reducers:
        {
            
        }
    }
)
 

export  default time.reducer
